#include <stdio.h>

int main()
{
    int numero = 1;

    printf("Múltiplos de 3 entre 1 y 100:\n");

    while (numero <= 100)
    {
        if (numero % 3 == 0)
        {
            printf("%d\n", numero);
        }
        numero++;
    }

    return 0;
}
